version: "3.9"
services:
  web:
    image: ${APP_IMAGE:-ghcr.io/g2bc/lumm-web}:${IMAGE_TAG:-latest}
    ports:
      - "4001:80"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
